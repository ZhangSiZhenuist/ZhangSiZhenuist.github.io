# 章思哲的个人博客

一个使用 React、Vinext 和 GitHub Pages 构建的轻量个人博客。

## 本地运行

```bash
npm install
npm run dev
```

## 发布

推送到 `main` 分支后，GitHub Actions 会自动构建并发布到 GitHub Pages。

首页内容位于 `app/page.tsx`，全局样式位于 `app/globals.css`。
